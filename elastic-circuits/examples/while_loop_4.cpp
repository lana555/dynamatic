//------------------------------------------------------------------------
// While loop
//------------------------------------------------------------------------

void while_loop(int a[], int b[], int c[]) {
    int i     = 0;
    int bound = 1000;
    int sum   = 0;

    while (sum < bound) {

        sum  = a[i] + b[i];
        c[i] = sum;
        i++;
    }
}
