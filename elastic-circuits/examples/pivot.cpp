//------------------------------------------------------------------------
// Pivot
//------------------------------------------------------------------------

void pivot(int x[], int a[], int n, int k) {
    int i;

    for (i = k + 1; i <= n; ++i) {

        x[k] = x[k] - a[i] * x[i];
    }
}
