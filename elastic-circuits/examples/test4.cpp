
int g(int i) {

    int k;

    if (i > 5) {
        k = i * i * i * i;
        k += 960;
        k = k >> 10;
        k += 1;
        k *= (i + 1) * (i + 1);
        k += 870;
        k = k >> 6;
        k += 1;
        k *= (i + 4) * (i + 4);
    } else {
        k = (i + 2) * (i + 2) * (i + 2) * (i + 2);
        k += 100;
        k = k >> 8;
        k += 1;
        k *= (i + 3) * (i + 3);
        k += 700;
        k = k >> 6;
        k += 1;
        k *= (i + 10) * (i + 10);
    }

    k++;

    return k;
}
